import { IExtractorLink } from './core/extractor_api.js';
declare global {
    function http_get(url: string, headers?: Record<string, string>): Promise<{
        status: number;
        body: string;
        headers: Record<string, string>;
    }>;
}
export declare class M3u8Helper {
    static getM3U8Qualities(masterUrl: string, headers?: Record<string, string>): Promise<IExtractorLink[]>;
}
