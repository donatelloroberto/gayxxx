export class ExtractorApi {
    fixUrl(url) {
        if (url.startsWith('http'))
            return url;
        if (url.startsWith('//'))
            return `https:${url}`;
        if (url.startsWith('/'))
            return `${this.mainUrl}${url}`;
        return `${this.mainUrl}/${url}`;
    }
}
